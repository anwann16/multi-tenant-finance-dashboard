import { NextResponse } from "next/server";

export async function GET() {
  return NextResponse.json({ message: "Better Auth catch-all route — to be implemented" });
}

export async function POST() {
  return NextResponse.json({ message: "Better Auth catch-all route — to be implemented" });
}
